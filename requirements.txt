# NTRIP Caster core dependencies - security patched versions
# Fixes multiple high and medium severity vulnerabilities (see SECURITY-VULNERABILITIES-FIX.md)

# Web framework core components
Flask==3.0.3           # Upgraded for compatibility with newer Werkzeug
Flask-SocketIO==5.3.6  # Current version has no known vulnerabilities
# Flask-CORS>=5.0.0      # Removed: CORS not needed, eliminates security vulnerabilities
python-socketio==5.8.0 # Current version has no known vulnerabilities
werkzeug>=3.0.6        # Fixes CVE-2024-34069, CVE-2024-49767, CVE-2024-49766, CVE-2023-46136
psutil==5.9.5          # System performance monitoring, no known vulnerabilities

# Build and package management tools
setuptools>=78.1.1     # Fixes CVE-2025-47273, CVE-2024-6345

# RTCM data processing
pyrtcm>=1.1.0          # RTCM message parsing library with MSM support
reverse-geocode>=1.6.0 # Offline reverse geocoding library
pyproj>=3.6.1          # Coordinate transformation library, no known vulnerabilities
